[package]
name = "bili-mate-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Engineering-only synthetic evaluator: feeds JSON fixtures through the clinical core (Stage 1 deliverable). Never deployed."

[[bin]]
name = "bili-eval"
path = "src/main.rs"

[dependencies]
clinical-core = { path = "../../crates/clinical-core" }
guideline-data = { path = "../../crates/guideline-data" }
serde = { workspace = true }
serde_json = { workspace = true }

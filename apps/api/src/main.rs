//! Axum API service — implemented in milestone M3.
//!
//! The Stage 1 deliverable is the deterministic clinical core plus the
//! engineering CLI evaluator (`apps/cli`). This binary exists only so the
//! workspace layout matches the system architecture specification.

fn main() {
    eprintln!(
        "bili-mate-api: not implemented until milestone M3; use apps/cli for Stage 1 evaluation"
    );
    std::process::exit(1);
}
